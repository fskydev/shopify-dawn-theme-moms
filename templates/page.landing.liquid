<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
<div class="landingpage_wrapper">
  <div class="d-flex flex-column justify-content-center w-100 h-100">

	<div class="d-flex flex-column justify-content-center align-items-center">
      <div class="container">
        <div class="tpvert">
          <h3>Hey mom, <br> welcome to your</h3>
          <h1>No Judgement <span>Club</span></h1>
		
			<div class="news_letter">
          			<div class="footer-block__newsletter">
            {%- if section.settings.newsletter_heading != blank -%}
              <h2 class="footer-block__heading">{{ section.settings.newsletter_heading | escape }}</h2>
            {%- endif -%}
            {%- form 'customer', id: 'ContactFooter', class: 'footer__newsletter newsletter-form' -%}
              <input type="hidden" name="contact[tags]" value="newsletter">
              <div class="newsletter-form__field-wrapper">
                <div class="field">
                  <input
                    id="NewsletterForm--{{ section.id }}"
                    type="email"
                    placeholder="Your email"
                    name="contact[email]"
                    class="field__input"
                    value="{{ form.email }}"
                    aria-required="true"
                    autocorrect="off"
                    autocapitalize="off"
                    autocomplete="email"
                    {% if form.errors %}
                      autofocus
                      aria-invalid="true"
                      aria-describedby="ContactFooter-error"
                    {% elsif form.posted_successfully? %}
                      aria-describedby="ContactFooter-success"
                    {% endif %}
                    placeholder="{{ 'newsletter.label' | t }}"
                    required
                  >
                  <button type="submit" class="newsletter-form__button field__button" name="commit" id="Subscribe" aria-label="{{ 'newsletter.button_label' | t }}">
                    <span>Request an invite</span>
                  </button>
                </div>
                {%- if form.errors -%}
                  <small class="newsletter-form__message form__message" id="ContactFooter-error">{% render 'icon-error' %}{{ form.errors.translated_fields['email'] | capitalize }} {{ form.errors.messages['email'] }}</small>
                {%- endif -%}
              </div>
              {%- if form.posted_successfully? -%}
                <h3 class="newsletter-form__message newsletter-form__message--success form__message" id="ContactFooter-success" tabindex="-1" autofocus>{% render 'icon-success' %}{{ 'newsletter.success' | t }}</h3>
              {%- endif -%}
            {%- endform -%}
          </div>
          	</div>
          
        </div>
      </div>
	</div>
  </div>
  
  <div class="footer_socail">
    <ul>
      <li><a href="#"> {%- render 'icon-facebook' -%}</a></li>
      <li><a href="#"> {%- render 'icon-instagram' -%}</a></li>
      <li><a href="#">{%- render 'icon-twitter' -%}</a></li>
    </ul>
    
 		
  </div>
  
</div>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600&display=swap');

  .landingpage_wrapper {
	background: rgb(248,205,111);
background: linear-gradient(90deg, rgba(248,205,111,1) 0%, rgba(239,188,157,1) 61%, rgba(219,190,220,1) 100%);
	background-size: 400% 400%;
	animation: gradient 15s ease infinite;
	height: 100vh;
}
  h1.fw-light.text-white.m-0 {
    padding: 0;
    margin: 0;
}

@keyframes gradient {
	0% {
		background-position: 0% 50%;
	}
	50% {
		background-position: 100% 50%;
	}
	100% {
		background-position: 0% 50%;
	}
}
  .tpvert h3 {
    color: #fff !important;
    font-family: Poppins !important;
    font-weight: 300 !important;
    font-size: 53px !important;
    line-height: 62px !important;
    margin-bottom: -4px !important;
}
  .tpvert h1 span {
    font-family: Poppins;
    text-transform: lowercase;
    font-weight: 300;
}
.tpvert h1 {
    font-family: 'Playfair Display';
    color: #fff;
    font-weight: 700 !important;
    font-size: 53px;
}
  .tpvert {
    padding-top: 190px;
}
  .footer_socail {
    text-align: right;
    margin-right: 42px;
}
.footer_socail li {
    display: inline-block;
    margin: 0px 5px;
}
.footer_socail svg {
    width: 23px;
    color: #fff !important;
}
  .footer_socail {
    position: relative;
    bottom: -213px;
}
.landingpage_wrapper {
    position: fixed;
    top: 0;
    z-index: 99;
    left: 0;
    right: 0;
}
.news_letter form#ContactFooter {
    flex-direction: column;
    margin-top: 28px;
}
.news_letter .newsletter-form__field-wrapper {
    max-width: 500px;
}  
.news_letter button#Subscribe {
    position: relative;
}
.news_letter button#Subscribe {
    font-family: Poppins;
    font-weight: 300;
} 
.news_letter input#NewsletterForm-- {
    box-shadow: none !important;
    border-bottom: 2px solid #fff;
}
.news_letter button#Subscribe {
    padding: 16px 15px !important;
    background: white;
    margin-left: 16px;
    border-radius: 6px;
    box-shadow: 0px 0px 6px 2px #0000000f;
}
.news_letter  .newsletter-form__button {
    width: 23.4rem;
    margin: 0;
}
.news_letter h3#ContactFooter-success {
    z-index: 99;
    position: relative;
}
  
  @media (max-width: 767px) {
  	.tpvert {
    	padding-top: 82px;
	}
    .tpvert h3 {
        color: #fff !important;
        font-family: Poppins !important;
        font-weight: 300 !important;
        font-size: 39px !important;
        line-height: 43px !important;
        margin-bottom: -4px !important;
	}
    .tpvert h1 {
    	font-family: 'Playfair Display';
    	font-size: 30px;
	}
    .news_letter button#Subscribe {
    padding: 16px 15px !important;
    background: white;
    margin-left: 13px;
    border-radius: 6px;
    box-shadow: 0px 0px 6px 2px #0000000f;
    font-size: 10px;
	}
  }
</style>